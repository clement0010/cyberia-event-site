query getOneParticipantDetails {
  participants {
    description
    name
    role
    team_id
    user_id
    team {
      motto
      name
      picture_url
    }
  }
}

query GetPublicLeaderboard {
  leaderboard_public {
    score
    team_id
    team_name {
      name
    }
  }
}
